<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ii3510.xhy0908.android_version1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >

        <activity  android:name=".SplashActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".WelcomeGuideActivity"
            android:theme="@android:style/Theme.Holo.Light.NoActionBar.Fullscreen"

            />

        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".HomeActivity"></activity>
        <activity android:name=".CourseRequestActivity"></activity>
        <activity android:name=".CourseResponseActivity"></activity>
        <activity android:name=".CourseDetailActivity"></activity>
        <activity android:name=".WriteCommentsActivity"></activity>
        <activity android:name=".CheckCommentsResponseActivity"></activity>
        <activity android:name=".CommentsDetailActivity"></activity>



    </application>

</manifest>